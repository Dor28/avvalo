# Shell scripts and nginx config run on the Linux server — force LF so a
# Windows CRLF checkout never produces a "bad interpreter" or parse error.
*.sh        text eol=lf
deploy/nginx/templates/*.template text eol=lf
*.png binary
*.jpg binary
*.jpeg binary
