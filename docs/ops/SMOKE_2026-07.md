# Avvalo Production Smoke Checklist - July 2026

> Created from `docs/ROADMAP.md` Phase A. This records the built baseline only;
> passing it does not prove Avvalo Verify exists. Do not claim or deploy new
> behavior without the branch-specific acceptance and authorization required by
> the repository rules.

## Phase A - Live Deployment Verification

- [ ] A1. Health: `curl https://<domain>/healthz` returns ok; founder-only: bot answers `/start` in Telegram.
- [ ] A2. Founder-only: full bot flow in `uz_latn`, `uz_cyrl`, and `ru` using inputs from `tests/fixtures/golden/checks.json`: `/start` -> consent -> text check -> result -> feedback buttons.
- [ ] A3. Founder-only: image path live with one real screenshot per script; OCR confidence logged; low-quality image returns the `low_ocr` path instead of crashing.
- [ ] A4. Live LLM quality: run `python tools/eval_models.py` against the production model config; founder-only: manually read Uzbek outputs for natural language, grounding, correct script, and no verdict words.
- [ ] A5. Web channel: if `WEB_ENABLED=true`, the landing page and `/check` work; consent is enforced; founder-only: Turnstile fires on image upload; limits return the friendly message. Confirm no merchant or scam-library product surface is exposed.
- [ ] A6. Founder-only: privacy audit on prod DB and logs: active product paths contain zero submitted content; the legacy `story_submission.minimized_text` table receives no new writes and remains covered by retention plus `/delete_my_data`; `docker logs` contain no user text or PII; `/delete_my_data` works end-to-end.
- [ ] A7. Founder-only: ops flags verified: `WEB_COOKIE_SECURE=true`; secrets are not in repo; retention job scheduled; DB backups are running and one restore test passed; cost-per-check logging is populated and <= $0.03; p90 latency <= 30s text / 45s image.
- [ ] A8. Founder-only: verify `OPERATOR_ALERT_CHAT_ID` is either intentionally configured for technical alerts or intentionally blank; it is not a story-review channel.

## Notes

- Evidence:
- Failures / follow-up:
