# Avvalo Production Smoke Checklist - July 2026

> Created from `docs/ROADMAP.md` Phase A on 2026-07-06. Building on
> `launch-features` may continue while these production checks remain open, but
> `main` must not be deployed until the founder records the evidence here.

## Phase A - Live Deployment Verification

- [ ] A1. Health: `curl https://<domain>/healthz` returns ok; founder-only: bot answers `/start` in Telegram.
- [ ] A2. Founder-only: full bot flow in `uz_latn`, `uz_cyrl`, and `ru` using golden-example inputs from `docs/FAMILY_VALIDATION.md` section 7: `/start` -> consent -> text check -> result -> feedback buttons.
- [ ] A3. Founder-only: image path live with one real screenshot per script; OCR confidence logged; low-quality image returns the `low_ocr` path instead of crashing.
- [ ] A4. Live LLM quality: run `python tools/eval_models.py` against the production model config; founder-only: manually read Uzbek outputs for natural language, grounding, correct script, and no verdict words.
- [ ] A5. Web channel: if `WEB_ENABLED=true`, anonymous text check works on `/` and `/merchants`; consent is enforced before processing; founder-only: Turnstile fires on image upload; over-limit path returns the friendly message.
- [ ] A6. Founder-only: privacy audit on prod DB and logs: `consent`, `check_event`, `feedback`, `rate_limit`, and `deletion_log` contain zero submitted content; `docker logs` contain no user text or PII; `/delete_my_data` works end-to-end.
- [ ] A7. Founder-only: ops flags verified: `WEB_COOKIE_SECURE=true`; secrets are not in repo; retention job scheduled; DB backups are running and one restore test passed; cost-per-check logging is populated and <= $0.03; p90 latency <= 30s text / 45s image.
- [ ] A8. Founder-only: set `OPERATOR_ALERT_CHAT_ID` to the founder's chat for R3 operator review forwarding.

## Notes

- Evidence:
- Failures / follow-up:
